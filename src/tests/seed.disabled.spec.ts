// placeholder seed file — intentionally disabled to avoid auto-execution
// original file moved to seed.disabled.spec.ts

/*
import { test, expect } from '@playwright/test';

test.describe('Test group', () => {
  test('seed', async ({ page }) => {
    // generate code here.
  });
});
*/
